---
title: "Vercel Workflow 30분 Step 운영 가이드: 실행 시간, 재시도, 비용 경계"
description: "Vercel Workflow step의 최대 실행 시간이 30분으로 늘어난 뒤 max duration, 재시도, idempotency, 취소와 관측성을 어떻게 설계해야 하는지 설명합니다."
date: 2026-07-26
verifiedAt: 2026-07-26
tags: ["Vercel", "Workflow", "AI Agent", "TypeScript", "Automation"]
---

Vercel Workflow의 step이 30분까지 실행될 수 있게 됐다고 해서 모든 긴 작업을 하나의 step에 넣어도 되는 것은 아닙니다. 이번 변경은 긴 LLM 호출, 문서 처리, 브라우저 자동화처럼 **800초 안에 끝나지 않던 단일 작업의 여유를 늘린 것**입니다. 워크플로 전체를 오래 유지하는 기능과, 한 step이 오래 실행되는 기능은 구분해서 설계해야 합니다.

Vercel은 2026년 7월 24일 Pro와 Enterprise 플랜의 Workflow step이 최대 1,800초까지 실행될 수 있다고 발표했습니다. 기존 상한은 800초였습니다. 기능은 beta이며 Fluid compute와 지원되는 Node.js 또는 Python 런타임이 필요합니다. Hobby 플랜은 300초 제한을 유지합니다.

이 글에서는 설정 방법보다 다음 질문에 답합니다.

- 30분 step이 필요한 작업과 쪼개야 하는 작업은 무엇인가
- 재시도 시 외부 작업이 중복 실행되지 않게 하려면 어떻게 하는가
- 긴 step을 취소하고 관측하려면 무엇을 기록해야 하는가
- 실행 시간이 늘어났을 때 비용과 장애 범위를 어떻게 제한하는가

## 워크플로 수명과 step 실행 시간을 구분합니다

Vercel Workflows는 대기, 승인, 재시작을 포함해 오랜 시간 상태를 유지할 수 있습니다. 하지만 실제 코드를 실행하는 각 step은 Vercel Function 호출입니다. 따라서 두 종류의 시간이 존재합니다.

| 시간 | 의미 | 적합한 처리 |
|---|---|---|
| 워크플로 전체 수명 | 여러 step과 대기 구간을 포함한 전체 흐름 | 승인 대기, 예약, webhook 대기, 여러 날의 상태 유지 |
| step 실행 시간 | 한 번의 함수 호출에서 코드가 실행되는 시간 | LLM 추론, OCR, 브라우저 작업, 대용량 변환 |

사용자 승인을 하루 동안 기다리는 작업은 30분 step이 필요하지 않습니다. 워크플로를 suspend한 뒤 응답이 오면 다음 step을 실행하면 됩니다. 반대로 하나의 외부 API가 15분 동안 스트리밍하고 결과를 한 번에 반환한다면 extended duration이 유용할 수 있습니다.

[AI SDK 7 프로덕션 에이전트 가이드](/blog/ai-sdk-7-production-agent-guide/)에서 설명한 `WorkflowAgent`도 같은 기준을 따릅니다. 에이전트 루프 전체를 한 함수에 가두는 것이 아니라, 도구 호출을 durable step으로 나누는 것이 핵심입니다.

## 먼저 프로젝트에서 extended duration을 활성화합니다

Workflow step의 30분 상한을 사용하려면 프로젝트 환경 변수에 다음 값을 추가하고 다시 배포합니다.

```text
VERCEL_ENABLE_WORKFLOW_EXTENDED_MAX_DURATION=1
```

조건은 다음과 같습니다.

- Pro 또는 Enterprise 플랜
- Fluid compute 활성화
- 지원되는 Node.js 또는 Python 런타임
- 환경 변수 추가 후 재배포

일반 Vercel Function은 함수 코드나 `vercel.json`의 `maxDuration`으로 실행 시간을 설정할 수 있습니다.

```ts
// app/api/long-task/route.ts
export const maxDuration = 1800;

export async function POST() {
  return Response.json({ ok: true });
}
```

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/long-task.py": {
      "maxDuration": 1800
    }
  }
}
```

다만 Workflow step의 opt-in과 일반 Function의 `maxDuration` 설정을 같은 것으로 보지 않는 편이 안전합니다. Workflow에는 Vercel이 안내한 환경 변수를 적용하고, 일반 route나 handler에는 필요할 때만 `maxDuration`을 명시하세요.

## 30분짜리 step이 적합한 작업

긴 step은 **중간 상태를 나누기 어렵고, 한 번의 외부 작업으로 완료되는 경우**에 적합합니다.

- 긴 LLM reasoning과 여러 차례의 tool call을 하나의 제공자 세션에서 수행
- 하나의 큰 PDF에 OCR과 구조 추출 실행
- 브라우저 세션을 유지하며 로그인이 필요한 작업 완료
- 영상·오디오 한 파일의 변환 또는 분석
- 외부 SaaS가 하나의 job을 동기식으로 완료할 때까지 대기

반대로 다음 작업은 여러 step으로 나누는 편이 좋습니다.

- 파일 수백 개를 동일한 방식으로 처리
- 서로 독립적인 URL을 순회하며 scraping
- 여러 고객이나 tenant의 데이터를 배치 처리
- 각 단계 결과를 검토하거나 재사용해야 하는 파이프라인
- 실패 시 처음부터 다시 실행하면 비용이 큰 작업

판단 기준은 실행 시간이 아니라 **재시도 단위와 복구 지점**입니다. 25분 걸리더라도 실패하면 처음부터 반복해야 하는 작업은 5분짜리 여러 step보다 위험할 수 있습니다.

## 긴 작업을 세 단계로 나누는 기본 패턴

문서 처리 에이전트를 예로 들면 다음처럼 나눌 수 있습니다.

```ts
import { getStepMetadata } from 'workflow';

export async function processDocumentWorkflow(documentId: string) {
  'use workflow';

  const source = await downloadDocument(documentId);
  const extracted = await extractDocument(source);
  const indexed = await indexDocument(documentId, extracted);

  return { documentId, indexed };
}

async function downloadDocument(documentId: string) {
  'use step';

  return fetchSource(documentId);
}

async function extractDocument(source: SourceDocument) {
  'use step';

  // OCR와 구조 추출처럼 오래 걸리는 단일 작업입니다.
  return runExtraction(source);
}

async function indexDocument(
  documentId: string,
  extracted: ExtractedDocument,
) {
  'use step';

  const { stepId } = getStepMetadata();

  return writeIndex({
    documentId,
    extracted,
    idempotencyKey: stepId,
  });
}
```

`extractDocument`가 20분 걸리더라도 다운로드와 인덱싱을 분리하면 다음 장점이 있습니다.

- 다운로드 성공 결과를 다시 사용할 수 있습니다.
- 추출 실패 시 외부 저장소를 다시 읽지 않습니다.
- 인덱싱 실패가 비싼 OCR 재실행으로 이어지지 않습니다.
- 각 step의 입력, 출력, 재시도와 지연 시간을 따로 볼 수 있습니다.

## 재시도를 고려해 idempotency key를 사용합니다

Workflow step은 실패하면 재시도될 수 있습니다. Vercel의 `WorkflowAgent` 문서에서는 도구 호출이 독립 step으로 실행되고 기본적으로 재시도된다고 설명합니다. 네트워크 오류 뒤 실제 외부 변경은 성공했는데 응답만 유실되면 같은 작업이 두 번 실행될 수 있습니다.

알림, 결제, 게시, 데이터 쓰기에는 안정적인 idempotency key가 필요합니다. `getStepMetadata()`가 제공하는 `stepId`는 재시도 사이에 유지되므로 외부 API의 idempotency key로 사용할 수 있습니다.

```ts
import { getStepMetadata } from 'workflow';

async function publishRelease(releaseId: string) {
  'use step';

  const { stepId } = getStepMetadata();

  const response = await fetch('https://api.example.com/releases', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Idempotency-Key': stepId,
    },
    body: JSON.stringify({ releaseId }),
  });

  if (!response.ok) {
    throw new Error(`publish failed: ${response.status}`);
  }

  return response.json();
}
```

외부 API가 idempotency key를 지원하지 않는다면 애플리케이션 데이터베이스에 `stepId`와 결과를 저장하고, 같은 key가 들어오면 기존 결과를 반환하는 방식을 사용합니다.

## 재시도 횟수는 작업 성격별로 다르게 둡니다

모든 오류를 세 번 반복하는 것은 안전하지 않습니다.

- 일시적인 429·5xx·네트워크 오류: 지연 후 재시도
- 입력 오류와 권한 오류: 즉시 실패
- 외부 쓰기 작업: idempotency가 보장될 때만 재시도
- 비용이 큰 LLM·OCR: 오류 유형과 이미 소비한 비용을 확인한 뒤 제한적으로 재시도

Workflow step은 `maxRetries`를 조정할 수 있습니다.

```ts
async function callFlakyProvider(input: ProviderInput) {
  'use step';

  return provider.run(input);
}

callFlakyProvider.maxRetries = 5;
```

중복 실행 위험이 있고 idempotency를 구현하지 못한 step은 재시도를 끄는 편이 낫습니다.

```ts
unsafeExternalWrite.maxRetries = 0;
```

30분 step이 다섯 번 재시도되면 최악의 경우 작업 하나가 오랫동안 자원과 외부 API 비용을 사용할 수 있습니다. 시간 상한과 재시도 상한을 함께 계산하세요.

## 취소는 시간 제한과 별도입니다

실행 시간이 길어질수록 사용자가 작업을 중단해야 하는 경우도 늘어납니다. 단순히 function timeout을 기다리게 하지 말고 `AbortController`와 `AbortSignal`을 전달해 진행 중인 외부 호출을 중단할 수 있게 합니다.

```ts
import { sleep } from 'workflow';

export async function cancellableWorkflow() {
  'use workflow';

  const controller = new AbortController();

  const result = await Promise.race([
    runLongAnalysis(controller.signal),
    sleep('20m').then(() => null),
  ]);

  if (result === null) {
    controller.abort('analysis exceeded workflow budget');
    return { status: 'cancelled' };
  }

  return { status: 'completed', result };
}
```

외부 SDK가 `AbortSignal`을 지원하지 않는다면 취소 요청이 실제 작업을 중단하지 못할 수 있습니다. 이 경우 provider job ID를 저장하고 별도의 cancel API를 호출해야 합니다.

## 30분을 그대로 timeout 값으로 쓰지 않습니다

플랫폼 상한이 1,800초라면 애플리케이션의 정상 작업 제한은 그보다 짧아야 합니다. 종료 직전에는 결과 저장, 로그 flush, cleanup 시간이 필요합니다.

예를 들어 다음처럼 예산을 나눌 수 있습니다.

| 구간 | 예시 예산 |
|---|---:|
| 외부 작업 | 20분 |
| 제한적 재시도 | 5분 |
| 결과 저장과 cleanup | 2분 |
| 안전 여유 | 3분 |

정상 작업이 매번 29분을 사용한다면 timeout을 늘린 것이 아니라 장애를 뒤로 미룬 것입니다. 입력 크기를 제한하거나 chunk 단위로 나누는 편이 낫습니다.

## 비용은 경과 시간만으로 추정하지 않습니다

Vercel은 Fluid compute에서 코드가 실제 CPU를 사용하는 시간과 provisioned memory를 기준으로 비용을 계산하며, 외부 모델이나 데이터베이스 응답을 기다리는 I/O 구간에는 active CPU 과금이 멈춘다고 설명합니다. 따라서 20분 동안 대부분 모델 스트림을 기다리는 step과 20분 동안 CPU로 영상을 변환하는 step은 비용 성격이 다릅니다.

운영 지표에는 다음 항목을 분리해 기록하세요.

- 전체 경과 시간
- active CPU 시간
- memory 사용량
- 모델·외부 API 비용
- 재시도 횟수와 누적 실행 시간
- 처리한 입력 크기

긴 실행을 허용하기 전에 실제 샘플 workload로 비용을 측정해야 합니다. 최대 실행 시간이 비용 예산을 자동으로 보장하지는 않습니다.

## 관측성은 run과 step을 연결합니다

Workflow dashboard는 step progression, payload, output, retry와 timing을 보여줍니다. Vercel Logs에서는 Workflow Run ID와 Workflow Step ID로 로그를 필터링할 수 있습니다.

애플리케이션 로그에도 같은 식별자를 포함하세요.

```ts
import { getStepMetadata } from 'workflow';

async function extractDocument(source: SourceDocument) {
  'use step';

  const metadata = getStepMetadata();
  const startedAt = Date.now();

  try {
    const result = await runExtraction(source);

    console.info('document extraction completed', {
      workflowRunId: metadata.workflowRunId,
      stepId: metadata.stepId,
      elapsedMs: Date.now() - startedAt,
      pageCount: source.pageCount,
    });

    return result;
  } catch (error) {
    console.error('document extraction failed', {
      workflowRunId: metadata.workflowRunId,
      stepId: metadata.stepId,
      elapsedMs: Date.now() - startedAt,
      error: error instanceof Error ? error.message : String(error),
    });

    throw error;
  }
}
```

민감한 문서 본문이나 모델 응답 전체를 로그에 저장하지 마세요. 식별자, 크기, 상태와 오류 유형만으로도 대부분의 운영 문제를 분석할 수 있습니다.

## 배포 전 실패 시나리오를 먼저 테스트합니다

최소한 다음 상황을 검증해야 합니다.

1. 800초를 넘지만 정상적으로 끝나는 step
2. 애플리케이션 timeout으로 중단되는 step
3. Vercel Function 상한에 도달하는 step
4. 외부 변경 성공 뒤 응답이 유실되어 재시도되는 step
5. 배포 또는 프로세스 재시작 뒤 다음 step으로 복구되는 workflow
6. 사용자 취소가 진행 중인 외부 요청까지 중단하는지
7. 동일 입력이 두 번 들어와도 결과가 중복되지 않는지

30분을 실제 테스트에서 기다리기 어렵다면 시간 의존 코드를 주입 가능한 clock이나 짧은 test budget으로 구성하세요. 다만 플랫폼의 실제 duration 제한은 별도의 staging 테스트로 확인해야 합니다.

## 운영 체크리스트

- [ ] Pro 또는 Enterprise 플랜과 Fluid compute 사용 여부를 확인했습니다.
- [ ] `VERCEL_ENABLE_WORKFLOW_EXTENDED_MAX_DURATION=1`을 설정하고 재배포했습니다.
- [ ] 긴 step이 하나의 원자적 작업인지 검토했습니다.
- [ ] 비싼 단계 전후에 복구 가능한 checkpoint가 있습니다.
- [ ] 외부 쓰기에 안정적인 idempotency key가 있습니다.
- [ ] 오류 유형별 재시도 횟수와 지연을 정했습니다.
- [ ] application timeout이 플랫폼 상한보다 짧습니다.
- [ ] 취소 신호가 실제 외부 작업까지 전달됩니다.
- [ ] run ID와 step ID로 로그를 연결할 수 있습니다.
- [ ] 재시도를 포함한 최악의 실행 시간과 비용을 계산했습니다.
- [ ] beta 기능 변경에 대비해 검증 날짜를 기록했습니다.

## 결론

Workflow step의 30분 지원은 긴 에이전트 작업을 억지로 여러 서버에 나누지 않아도 되는 범위를 넓혔습니다. 하지만 가장 좋은 step은 오래 실행되는 step이 아니라 **실패했을 때 안전하게 다시 실행할 수 있는 단위**입니다.

긴 단일 외부 작업에는 extended duration을 사용하되, 여러 독립 작업은 durable step으로 나누세요. 쓰기 작업에는 idempotency key를 붙이고, 재시도·취소·application timeout을 플랫폼 상한과 별도로 관리해야 합니다. 마지막으로 30분 상한을 활성화하기 전에 staging에서 실제 비용과 복구 동작을 측정하세요.

## 공식 자료

- [Workflow steps now support extended function durations](https://vercel.com/changelog/workflow-steps-now-support-extended-function-durations)
- [Vercel Functions can now run up to 30 minutes](https://vercel.com/changelog/vercel-functions-can-now-run-up-to-30-minutes)
- [Configuring Maximum Duration for Vercel Functions](https://vercel.com/docs/functions/configuring-functions/duration)
- [What is WorkflowAgent?](https://vercel.com/kb/guide/what-is-workflowagent)
- [Workflow SDK inflight cancellation](https://vercel.com/changelog/workflow-sdk-now-supports-inflight-cancellation)
- [Fluid compute pricing](https://vercel.com/docs/functions/usage-and-pricing)
- [Workflow logs filtering](https://vercel.com/changelog/logs-filtering-for-vercel-workflows-now-available)
