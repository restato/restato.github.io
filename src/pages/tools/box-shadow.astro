---
import MainLayout from '../../layouts/MainLayout.astro';
import BoxShadowGenerator from '../../components/tools/BoxShadowGenerator';
---

<MainLayout
  title="CSS Box Shadow 생성기 | Restato"
  description="CSS box-shadow를 시각적으로 생성합니다. 여러 레이어의 그림자를 만들 수 있습니다."
>
  <div class="max-w-4xl mx-auto px-4 py-8">
    <!-- Breadcrumb -->
    <nav class="text-sm mb-6">
      <ol class="flex items-center gap-2">
        <li><a href="/" class="text-[var(--color-text-muted)] hover:text-primary-500">홈</a></li>
        <li class="text-[var(--color-text-muted)]">/</li>
        <li><a href="/tools" class="text-[var(--color-text-muted)] hover:text-primary-500">도구</a></li>
        <li class="text-[var(--color-text-muted)]">/</li>
        <li class="text-[var(--color-text)]">Box Shadow 생성기</li>
      </ol>
    </nav>

    <!-- Header -->
    <header class="mb-8">
      <h1 class="text-3xl font-bold text-[var(--color-text)] mb-4">
        🎭 CSS Box Shadow 생성기
      </h1>
      <p class="text-[var(--color-text-muted)]">
        CSS box-shadow를 시각적으로 생성합니다. 레이어를 추가해 복잡한 그림자를 만들 수 있습니다.
      </p>
    </header>

    <!-- Tool -->
    <div class="p-6 rounded-xl bg-[var(--color-card)] border border-[var(--color-border)]">
      <BoxShadowGenerator client:load />
    </div>

    <!-- Back Link -->
    <div class="mt-8">
      <a href="/tools" class="text-primary-500 hover:underline">← 도구 목록으로 돌아가기</a>
    </div>
  </div>
</MainLayout>
