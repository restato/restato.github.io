---
interface Props {
  size?: 'reading' | 'content' | 'wide';
  class?: string;
}

const { size = 'content', class: className } = Astro.props;
---

<main class:list={['fc-page', `fc-${size}`, className]}>
  <slot />
</main>
