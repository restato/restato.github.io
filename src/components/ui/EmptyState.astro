---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<section class="fc-empty-state">
  <h2>{title}</h2>
  {description && <p>{description}</p>}
  <slot name="action" />
</section>
